# `vicinae-crypto`

🔑 A Vicinae extension providing cryptography features such as calculating hashes and generating UUIDs.

## Installation

To install this extension, clone this repository and build the extension using the following command. Upon build, the extension is automatically installed.

```sh
npm run build
```
